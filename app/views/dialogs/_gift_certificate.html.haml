#viel-modal.OrangeRed.modal.hide.fade{:tabindex=>"-1", :role=>"dialog", :'aria-labelledby'=>"my_modal_label", :'aria-hidden'=>"true", :method => 'post', :action => 'mailto: parshukova.elena@gmail.com'}
  .modal-header
    .button{:type=>"button", :class=>"close", :'data-dismiss'=>"modal", :'aria-hidden'=>"true"}×
    %h3#my_modal_label
      Заказ подарочного сертификата

  .modal-body
    %h8.italic 
      Стоимость подарочного сертификата от 500 руб. Бесплатная доставка по городу при покупки от 5000 руб. Вы всегда можете приобрести подарочный сертификат на меньшую сумму у нас в спа-центре. 
    %br
    %br
    = form_for GiftCertificate.new, :url => gift_certificates_path, :html => {:class => 'form-vertical'} do |f|
      -# блок для имени
      .control-group
        .controls
          = f.text_field :name, :placeholder => 'Имя*', :required => 'Ваше имя'

      -# блок для телефона
      .control-group
        .controls
          = f.text_field :phone, :placeholder => 'Контактный телефон*', :required => 'Ваш телефон'

      -# блок для времени звонка
      .control-group
        .controls
          = f.text_area :time, :placeholder => 'В какое время Вам лучше позвонить?'

      -# блок для e-mail
      .control-group
        .controls
          = f.email_field :email, :placeholder => 'Ваш e-mail'

      %h8.italic ВНИМАНИЕ! Поля, помеченные *, обязательны для заполнения

  .modal-footer
    .button.btn.btn-primary#submit_form Отправить заказ

:javascript
  $('#submit_form').click(function(){
    if (!$.trim($('#gift_certificate_name').val())) {
      $('#gift_certificate_name').focus();
      return;
    }

    if (!$.trim($('#gift_certificate_phone').val())) {
      $('#gift_certificate_phone').focus();
      return;
    }
    $('form#new_gift_certificate').submit();
  });
